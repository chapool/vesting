# HZ Token 部署指南

## 文档概述

本文档提供 HZ Token 生态系统合约的完整部署指南，包括环境准备、部署流程、配置方法和验证步骤。

**文档版本**: v2.0  
**最后更新**: 2025年8月1日

---

## 目录

1. [环境准备](#1-环境准备)
2. [部署流程](#2-部署流程)
3. [配置设置](#3-配置设置)
4. [验证检查](#4-验证检查)
5. [升级管理](#5-升级管理)
6. [故障排除](#6-故障排除)

---

## 1. 环境准备

### 1.1 系统要求

**开发环境**:
- Node.js >= 16.0.0
- npm >= 8.0.0 或 yarn >= 1.22.0
- Git >= 2.20.0

**网络要求**:
- 稳定的网络连接
- 足够的 ETH 用于 Gas 费用
- 访问以太坊节点 (Infura/Alchemy)

### 1.2 项目初始化

```bash
# 克隆项目
git clone <repository-url>
cd hz-token-hardhat

# 安装依赖
npm install

# 编译合约
npm run compile

# 运行测试
npm run test
```

### 1.3 环境配置

**创建 .env 文件**:
```bash
# 网络配置
INFURA_API_KEY=your_infura_api_key
ALCHEMY_API_KEY=your_alchemy_api_key

# 私钥配置 (请使用环境变量，不要提交到代码库)
DEPLOYER_PRIVATE_KEY=your_deployer_private_key
OWNER_PRIVATE_KEY=your_owner_private_key

# 合约验证
ETHERSCAN_API_KEY=your_etherscan_api_key

# 多签钱包地址
MULTISIG_WALLET=0x...
TEAM_WALLET=0x...
ECOSYSTEM_WALLET=0x...
```

**Hardhat 配置**:
```javascript
// hardhat.config.js
require("@nomicfoundation/hardhat-toolbox");
require("dotenv").config();

module.exports = {
  solidity: {
    version: "0.8.19",
    settings: {
      optimizer: {
        enabled: true,
        runs: 200
      }
    }
  },
  networks: {
    hardhat: {
      forking: {
        url: `https://mainnet.infura.io/v3/${process.env.INFURA_API_KEY}`,
        blockNumber: 18000000
      }
    },
    mainnet: {
      url: `https://mainnet.infura.io/v3/${process.env.INFURA_API_KEY}`,
      accounts: [process.env.DEPLOYER_PRIVATE_KEY],
      gasPrice: 20000000000, // 20 gwei
      gas: 6000000
    },
    sepolia: {
      url: `https://sepolia.infura.io/v3/${process.env.INFURA_API_KEY}`,
      accounts: [process.env.DEPLOYER_PRIVATE_KEY],
      gasPrice: 20000000000,
      gas: 6000000
    }
  },
  etherscan: {
    apiKey: process.env.ETHERSCAN_API_KEY
  }
};
```

---

## 2. 部署流程

### 2.1 部署顺序

合约必须按以下顺序部署以确保依赖关系正确：

```
1. Constants.sol       (系统常量)
   ↓
2. HZToken.sol        (核心代币合约)  
   ↓
3. Vesting.sol        (释放管理合约)
   ↓
4. MiningPool.sol     (挖矿管理合约)
   ↓
5. 权限配置和关联设置
   ↓
6. 代币铸造和初始化
```

### 2.2 部署脚本

**主部署脚本 (scripts/deploy.js)**:
```javascript
const { ethers } = require("hardhat");
const { parseEther } = ethers.utils;

async function main() {
  console.log("开始部署 HZToken 生态系统...");
  
  const [deployer] = await ethers.getSigners();
  console.log("部署地址:", deployer.address);
  console.log("余额:", ethers.utils.formatEther(await deployer.getBalance()));

  // 1. 部署 Constants
  console.log("\n1. 部署 Constants 合约...");
  const Constants = await ethers.getContractFactory("Constants");
  const constants = await Constants.deploy();
  await constants.deployed();
  console.log("Constants 地址:", constants.address);

  // 2. 部署 HZToken (可升级)
  console.log("\n2. 部署 HZToken 合约...");
  const HZToken = await ethers.getContractFactory("HZToken");
  const hzToken = await upgrades.deployProxy(
    HZToken, 
    ["HZ Token", "HZ", vesting.address], 
    { initializer: 'initialize' }
  );
  await hzToken.deployed();
  console.log("HZToken 地址:", hzToken.address);

  // 3. 部署 Vesting
  console.log("\n3. 部署 Vesting 合约...");
  const Vesting = await ethers.getContractFactory("Vesting");
  const vesting = await Vesting.deploy(hzToken.address);
  await vesting.deployed();
  console.log("Vesting 地址:", vesting.address);

  // 4. 部署 MiningPool
  console.log("\n4. 部署 MiningPool 合约...");
  const MiningPool = await ethers.getContractFactory("MiningPool");
  const miningPool = await MiningPool.deploy(
    vesting.address,
    hzToken.address,
    process.env.MULTISIG_WALLET
  );
  await miningPool.deployed();
  console.log("MiningPool 地址:", miningPool.address);

  // 保存部署地址
  const deploymentInfo = {
    network: network.name,
    timestamp: new Date().toISOString(),
    deployer: deployer.address,
    contracts: {
      Constants: constants.address,
      HZToken: hzToken.address,
      Vesting: vesting.address,
      MiningPool: miningPool.address
    }
  };

  await saveDeploymentInfo(deploymentInfo);
  
  console.log("\n✅ 所有合约部署完成！");
  return deploymentInfo;
}

async function saveDeploymentInfo(info) {
  const fs = require('fs');
  const path = `deployments/${info.network}-deployment.json`;
  
  if (!fs.existsSync('deployments')) {
    fs.mkdirSync('deployments');
  }
  
  fs.writeFileSync(path, JSON.stringify(info, null, 2));
  console.log(`部署信息已保存到: ${path}`);
}

main().catch((error) => {
  console.error("部署失败:", error);
  process.exitCode = 1;
});
```

### 2.3 执行部署

**测试网部署**:
```bash
# Sepolia 测试网
npm run deploy:sepolia

# 或者使用 hardhat
npx hardhat run scripts/deploy.js --network sepolia
```

**主网部署**:
```bash
# 主网部署 (谨慎操作)
npm run deploy:mainnet

# 或者
npx hardhat run scripts/deploy.js --network mainnet
```

---

## 3. 配置设置

### 3.1 权限配置

**配置脚本 (scripts/configure.js)**:
```javascript
async function configurePermissions(contracts) {
  const { hzToken, vesting, miningPool } = contracts;
  
  console.log("配置权限...");
  
  // HZToken 权限配置
  const MINTER_ROLE = await hzToken.MINTER_ROLE();
  const PAUSER_ROLE = await hzToken.PAUSER_ROLE();
  const BLACKLIST_MANAGER_ROLE = await hzToken.BLACKLIST_MANAGER_ROLE();
  
  // 给 Vesting 合约铸造权限
  await hzToken.grantRole(MINTER_ROLE, vesting.address);
  console.log("✅ Vesting 合约获得铸造权限");
  
  // 配置管理员权限
  await hzToken.grantRole(PAUSER_ROLE, process.env.MULTISIG_WALLET);
  await hzToken.grantRole(BLACKLIST_MANAGER_ROLE, process.env.MULTISIG_WALLET);
  console.log("✅ 多签钱包获得管理权限");
  
  // MiningPool 权限配置
  await miningPool.addApprover(process.env.MULTISIG_WALLET);
  await miningPool.addBatchExecutor(process.env.MULTISIG_WALLET);
  console.log("✅ MiningPool 权限配置完成");
}
```

### 3.2 代币铸造和分发

```javascript
async function mintAndDistribute(contracts) {
  const { hzToken, vesting } = contracts;
  
  console.log("铸造代币...");
  
  // 铸造全部代币到 Vesting 合约
  const totalSupply = parseEther("10000000000"); // 100亿代币
  await hzToken.mint(vesting.address, totalSupply);
  
  console.log("✅ 代币铸造完成");
  console.log("总供应量:", ethers.utils.formatEther(await hzToken.totalSupply()));
}
```

### 3.3 创建 Vesting 计划

```javascript
async function createVestingSchedules(vesting, miningPool) {
  console.log("创建 Vesting 计划...");
  
  const now = Math.floor(Date.now() / 1000);
  
  // 1. 挖矿奖励计划 (50%)
  await vesting.createVestingSchedule(
    miningPool.address,      // 受益人: MiningPool
    now,                     // 立即开始
    0,                       // 无 cliff
    6 * 365 * 24 * 3600,    // 6年
    30 * 24 * 3600,         // 30天间隔
    false,                   // 不可撤销
    parseEther("5000000000") // 50亿代币
  );
  console.log("✅ 挖矿奖励计划创建完成");
  
  // 2. 运营生态计划 (20%)
  await vesting.createVestingSchedule(
    process.env.ECOSYSTEM_WALLET,
    now,
    0,
    5 * 365 * 24 * 3600,    // 5年
    30 * 24 * 3600,
    true,                    // 可撤销
    parseEther("2000000000") // 20亿代币
  );
  console.log("✅ 运营生态计划创建完成");
  
  // 3. 团队激励计划 (10%)
  await vesting.createVestingSchedule(
    process.env.TEAM_WALLET,
    now,
    365 * 24 * 3600,        // 1年 cliff
    3 * 365 * 24 * 3600,    // 3年总周期
    30 * 24 * 3600,
    true,
    parseEther("1000000000") // 10亿代币
  );
  console.log("✅ 团队激励计划创建完成");
  
  // 基石轮计划需要根据投资协议单独创建
  console.log("⏳ 基石轮计划待投资协议确定后创建");
}
```

### 3.4 完整配置脚本

```javascript
// scripts/post-deploy-config.js
async function main() {
  // 加载部署信息
  const deployment = require(`../deployments/${network.name}-deployment.json`);
  
  // 获取合约实例
  const contracts = await getContractInstances(deployment.contracts);
  
  // 执行配置
  await configurePermissions(contracts);
  await mintAndDistribute(contracts);
  await createVestingSchedules(contracts.vesting, contracts.miningPool);
  
  console.log("🎉 配置完成！");
}

// 执行: npx hardhat run scripts/post-deploy-config.js --network sepolia
```

---

## 4. 验证检查

### 4.1 合约验证清单

**基础验证**:
```javascript
async function basicVerification(contracts) {
  const { hzToken, vesting, miningPool } = contracts;
  
  console.log("执行基础验证...");
  
  // 1. 代币基本信息
  const name = await hzToken.name();
  const symbol = await hzToken.symbol();
  const totalSupply = await hzToken.totalSupply();
  const decimals = await hzToken.decimals();
  
  console.log(`代币名称: ${name}`);
  console.log(`代币符号: ${symbol}`);
  console.log(`总供应量: ${ethers.utils.formatEther(totalSupply)}`);
  console.log(`小数位数: ${decimals}`);
  
  // 2. Vesting 合约验证
  const vestingTotal = await vesting.getVestingSchedulesTotalAmount();
  console.log(`Vesting 总锁定: ${ethers.utils.formatEther(vestingTotal)}`);
  
  // 3. 权限验证
  const MINTER_ROLE = await hzToken.MINTER_ROLE();
  const hasMinterRole = await hzToken.hasRole(MINTER_ROLE, vesting.address);
  console.log(`Vesting 是否有铸造权限: ${hasMinterRole}`);
  
  return {
    name, symbol, totalSupply, decimals,
    vestingTotal, hasMinterRole
  };
}
```

**功能验证**:
```javascript
async function functionalVerification(contracts) {
  console.log("执行功能验证...");
  
  // 测试转账功能
  const [owner] = await ethers.getSigners();
  const testAmount = parseEther("1000");
  
  // 从 owner 转账给测试地址
  const testAddress = "0x..."; // 测试地址
  await hzToken.transfer(testAddress, testAmount);
  
  const balance = await hzToken.balanceOf(testAddress);
  console.log(`测试转账成功: ${ethers.utils.formatEther(balance)} HZ`);
  
  // 测试 Vesting 释放
  // ... 其他功能测试
}
```

### 4.2 安全检查

```javascript
async function securityCheck(contracts) {
  console.log("执行安全检查...");
  
  const { hzToken, vesting, miningPool } = contracts;
  
  // 1. 检查合约所有权
  const tokenOwner = await hzToken.owner();
  const vestingOwner = await vesting.owner();
  const poolOwner = await miningPool.owner();
  
  console.log("合约所有者:");
  console.log(`HZToken: ${tokenOwner}`);
  console.log(`Vesting: ${vestingOwner}`);
  console.log(`MiningPool: ${poolOwner}`);
  
  // 2. 检查权限配置
  const roles = [
    await hzToken.MINTER_ROLE(),
    await hzToken.PAUSER_ROLE(),
    await hzToken.BLACKLIST_MANAGER_ROLE()
  ];
  
  for (const role of roles) {
    const members = await hzToken.getRoleMemberCount(role);
    console.log(`角色 ${role} 成员数量: ${members}`);
  }
  
  // 3. 检查合约状态
  const isPaused = await hzToken.paused();
  console.log(`合约是否暂停: ${isPaused}`);
}
```

### 4.3 Etherscan 验证

```bash
# 验证合约源码
npx hardhat verify --network mainnet DEPLOYED_CONTRACT_ADDRESS "Constructor Arg 1" "Constructor Arg 2"

# 或者使用脚本批量验证
npm run verify:mainnet
```

**验证脚本**:
```javascript
// scripts/verify.js
async function verifyContracts() {
  const deployment = require(`../deployments/${network.name}-deployment.json`);
  
  try {
    // 验证 HZToken (可升级合约需要特殊处理)
    // 可升级合约验证需要验证实现合约，不是代理合约
    await hre.run("verify:verify", {
      address: deployment.contracts.HZTokenImplementation,
      constructorArguments: []
    });
    
    // 验证 Vesting
    await hre.run("verify:verify", {
      address: deployment.contracts.Vesting,
      constructorArguments: [deployment.contracts.HZToken]
    });
    
    // 验证其他合约...
    console.log("✅ 所有合约验证完成");
  } catch (error) {
    console.error("验证失败:", error);
  }
}
```

---

## 5. 升级管理

### 5.1 代理合约部署

如果使用可升级代理模式：

```javascript
// scripts/deploy-upgradeable.js
const { upgrades } = require('@openzeppelin/hardhat-upgrades');

async function deployUpgradeable() {
  const HZToken = await ethers.getContractFactory("HZToken");
  
  // 部署代理合约
  const hzToken = await upgrades.deployProxy(
    HZToken, 
    ["HZ Token", "HZ", vestingAddress], 
    { initializer: 'initialize' }
  );
  
  await hzToken.deployed();
  console.log("代理合约地址:", hzToken.address);
  
  return hzToken;
}
```

### 5.2 合约升级

```javascript
async function upgradeContract(proxyAddress) {
  const HZTokenV2 = await ethers.getContractFactory("HZTokenV2");
  
  const upgraded = await upgrades.upgradeProxy(proxyAddress, HZTokenV2);
  console.log("合约升级完成:", upgraded.address);
}
```

---

## 6. 故障排除

### 6.1 常见问题

**Gas 费用不足**:
```bash
Error: insufficient funds for gas * price + value
```
解决方案: 确保部署地址有足够的 ETH

**网络连接问题**:
```bash
Error: network connection timeout
```
解决方案: 检查网络配置和 API 密钥

**合约验证失败**:
```bash
Error: contract verification failed
```
解决方案: 检查构造函数参数和编译器版本

### 6.2 调试工具

**本地调试**:
```bash
# 启动本地节点
npx hardhat node

# 部署到本地网络
npx hardhat run scripts/deploy.js --network localhost
```

**Fork 主网调试**:
```javascript
// hardhat.config.js
networks: {
  hardhat: {
    forking: {
      url: "https://mainnet.infura.io/v3/YOUR-PROJECT-ID",
      blockNumber: 18000000
    }
  }
}
```

### 6.3 监控和日志

**部署监控脚本**:
```javascript
async function monitorDeployment(txHash) {
  console.log(`监控交易: ${txHash}`);
  
  const receipt = await ethers.provider.waitForTransaction(txHash);
  
  if (receipt.status === 1) {
    console.log("✅ 交易成功");
  } else {
    console.log("❌ 交易失败");
  }
  
  console.log(`Gas 消耗: ${receipt.gasUsed}`);
  console.log(`区块号: ${receipt.blockNumber}`);
}
```

---

## 部署检查清单

### 部署前检查
- [ ] 环境配置正确
- [ ] 私钥安全存储
- [ ] 足够的 ETH 余额
- [ ] 合约代码审计完成
- [ ] 测试网部署验证

### 部署后检查
- [ ] 合约地址正确
- [ ] 权限配置正确
- [ ] 代币总量正确
- [ ] Vesting 计划正确
- [ ] 源码验证成功

### 安全检查
- [ ] 多签钱包配置
- [ ] 权限最小化原则
- [ ] 紧急暂停机制
- [ ] 监控告警设置
- [ ] 备份和恢复计划

---

**相关工具**:
- [Hardhat](https://hardhat.org/) - 开发框架
- [OpenZeppelin](https://openzeppelin.com/) - 安全合约库
- [Etherscan](https://etherscan.io/) - 合约验证
- [Tenderly](https://tenderly.co/) - 调试和监控

---

*文档版本: v2.0*  
*最后更新: 2025年8月1日*